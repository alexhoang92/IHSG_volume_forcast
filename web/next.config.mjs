/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow plain <img> tags for external chart images served from Railway
  // (avoids needing to configure next/image for dynamic external domains)
};

export default nextConfig;
